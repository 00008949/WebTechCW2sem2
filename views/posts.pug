extends layout.pug

block content
    div.container
        h1.postsH1 All posts

        each post in posts
            .post-wrapper
                a.post(href=`/blog-app/${ posts.id }`) #{ post.title}
                a.delete(href='#') delete
